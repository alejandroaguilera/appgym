DATABASE_URL="postgresql://user:password@host:5432/appgym"

# Mensaje motivador al cerrar la semana (xAI / Grok). Opcional: sin esta llave
# la celebración sigue funcionando con un mensaje determinista armado a partir
# de las mismas estadísticas.
XAI_API_KEY=""
# Override del modelo, por si cambia el id. Default: grok-4.5
XAI_MODEL=""
